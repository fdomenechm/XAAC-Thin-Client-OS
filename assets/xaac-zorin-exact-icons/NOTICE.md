# XAAC Zorin exact icon subset

This directory contains only the symbolic icons referenced by XAAC Thin Client. Each SVG is copied byte-for-byte (dereferencing aliases) from the effective ZorinBlue-Light → Zorin → Adwaita inheritance chain exported from the development machine.
