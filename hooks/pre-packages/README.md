# Hook pre-packages

Directori reservat per als scripts que s'executaran durant l'etapa `pre-packages` del procés de construcció.

Es manté versionat encara que no hi haja cap hook configurat en la fase actual.
